<% layout("/layouts/boilerplate") -%>
<div class="row mt-3">
  <div class="col-10 col-md-8 mx-auto">
    <h2 style="text-align: center"><%= listing.title %></h2>
  </div>

  <div class="card col-10 col-md-6 mx-auto mt-3 show-card listing-card">
    <% if (listing) { %>

    <img
      src="<%= listing.image%>"
      class="card-img-top show-img"
      alt="listing_image"
    />
    <div class="card-body">
      <p class="card-text">
        <%= listing.description %> <br />
        &#8377; <%= Number(listing.price).toLocaleString("en-IN") %><br />
        <%= listing.location %> <br />
        <%= listing.country %> <br />
      </p>
    </div>
  </div>
  <% } else { %>
  <p>Listing not found</p>
  <% } %>
  <br />

  <div class="btns mt-3 mb-2 d-flex justify-content-center">
    <a
      href="/listings/<%= listing._id %>/edit"
      class="btn btn-dark mx-2 edit-btn"
    >
      Edit
    </a>
    <form method="POST" action="/listings/<%= listing._id%>?_method=DELETE">
      <button class="btn btn-dark mx-2 delete-btn">Delete</button>
    </form>
  </div>

  <div class="col-10 col-md-8 mx-auto mb-3">
    <hr />
    <h4>Leave a Review</h4>
    <form
      action="/listings/<%= listing.id %>/reviews"
      method="POST"
      novalidate
      class="needs-validation"
    >
      <div class="mb-3 mt-3">
        <label for="rating" class="form-label">Rating</label>
        <input
          type="range"
          min="1"
          max="5"
          id="rating"
          name="review[rating]"
          class="form-range"
        />
      </div>
      <div class="mb-3 mt-3">
        <label for="comment" class="form-label">Comment</label>
        <textarea
          name="review[comment]"
          id="comment"
          cols="30"
          rows="5"
          class="form-control"
          required
        ></textarea>
        <div class="invalid-feedback">Please leave some Comments.</div>
      </div>
      <button class="btn btn-outline-dark">Submit</button>
    </form>
    <hr />
    <p><b>All Reviews</b></p>
    <div class="row">
      <% for(review of listing.reviews) { %>
      <div class="card col-5 ms-3 mb-3">
        <div class="card-body d-flex flex-column">
          <h4 class="card-title">Sanjana</h4>
          <p class="card-text"><%=review.comment%></p>
          <p class="card-text"><%=review.rating%><i class="fa fa-star"></i></p>
        </div>
      </div>

      <% } %>
    </div>
  </div>
</div>
